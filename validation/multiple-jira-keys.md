# Ambiguous issue mapping validation

The pull request title for this controlled case references two Jira issues. SpecGuard must refuse
to guess which requirement owns the change and must stop before creating a run or invoking the
model.
