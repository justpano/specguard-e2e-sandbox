# Ingress validation

This documentation-only change intentionally omits a Jira issue key from both the branch and pull
request metadata. SpecGuard must reject it before creating an analysis run or invoking the model.
